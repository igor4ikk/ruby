<h2 class = "text-center">show element</h2>

<div>
    <p>Title: <%= @post.title%></p>
    <p>Body: <%= @post.body%></p>
    <p><%= image_tag @post.preview_img if @post.preview_img.present? %></p>
</div>
<%= link_to "Edit", edit_admin_post_path(@post.id), class: 'btn btn-secondary'%>